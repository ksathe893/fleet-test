namespace: fleet-secret-test
helm:
  chart: .
diff:
  comparePatches:
    - apiVersion: v1
      kind: Secret
      name: existing-secret
      namespace: fleet-secret-test
      operations:
        - op: ignore
